Test Case One
- **Test Case Description**: Retrieving a random cat fact.
- **Expected Result**: API returns a non-empty string.
- **Validation**: Check if returned data is a non-empty string.
- **Reason**: Ensures that the API response contains meaningful data  and not just an empty string
Test Case Two
- **Test Case Description**:Retrieving fact type is cat
- **Expected Result**: API returns cat facts only
- **Validation**: Validate that all returned facts have the type 'cat'
- **Reason**: Verifies that the API correctly returns cat facts and not other animals